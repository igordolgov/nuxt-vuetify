// ГЛОБАЛЬНАЯ ШИНА СОБЫТИЙ. Это новый экземпляр Vue, который
// не подключается ни к какому  DOM-элементу, а просто используется
// его механизм для работы с событиями Vue (с глобальными)
import Vue from 'vue';

export default new Vue();
